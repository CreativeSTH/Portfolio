import React from "react";
import '../styles/SocketFooter.css';

const Socket = () => {
    return(
        <section className="sockect">
            <p> This amazing website has been created with only<span className="highlight"> Modern CSS</span> without using <span className="highlight"> JavaScript</span> on animations.</p>
        </section>
    );
};

export default Socket;